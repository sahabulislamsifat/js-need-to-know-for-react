// And (&&), Or (||)

const person = {
  name: "Sifat",
  gender: "male",
  isStudent: true,
  //   isStudent: false,
};

// console.log(person.isStudent && "He is a Student.");
console.log(person.name || "Not Available.");
